#image_numbers:
    #start:
    #end:
    #step:
# start, end and step defining the image numbers of diffraction patterns to analyse  

file_paths:
    input_text_path: ../../../SXRD_analysis/diamond_2021/{experiment_number:05d}/texture-cpf/{experiment_number:05d}-{intensity_type}-intensities/{experiment_number:05d}_peak_intensity_{label}.txt
# input path of the texture text file, excluding the experiment number, intensity type and lattice plane peak label.

    output_text_path: ../../../SXRD_analysis/diamond_2021/combined-intensities-MTEX/{experiment_start}-to-{experiment_end}-combined-{intensity_type}-intensities/combined_peak_intensity_{label}.txt
# output path for the texture text file, excluding the experiment number, intensity type and lattice plane peak label.

user_inputs:
    experiment_numbers:
     - 103840
     - 103841
     - 103842
     - 103843
     - 103844
     - 103845
#      - 103846
#      - 103847
#      - 103848
#      - 103849
#      - 103850
#      - 103851
# list of experiment numbers for the sample intensity data to combine  
    
    intensity_type: raw
#     intensity_type: powder-corrected
# intensity type of the data to combine, options inlude 'raw' or 'powder-corrected'.